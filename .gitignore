# SQLite databases
*.db
*.sqlite
*.sqlite3
*.db-journal
*.db-shm
*.db-wal
workspace.db

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/

# Uploaded Media
uploads/
weights/
*.pt

# Secrets — never commit. (.jwt_secret was tracked historically; see docs/GOTCHAS.md #1)
.jwt_secret
.env

# macOS
.DS_Store

# Model weights — large binaries, fetched or placed manually.
# Layout: models/detector/ (YOLO detect+seg), models/wand/ (SAM variants)
models/**/*.onnx
models/**/*.pt

# Agent working notes — not part of the shipped codebase
.devnotes/

# Application logs (rotating app.log)
logs/

# Local database backups (contain real annotation data)
backups/

# Team Credentials
team_credentials.txt

# Generated by scripts/install-service.ps1 on each run; not source.
scripts/_service-wrapper.ps1
